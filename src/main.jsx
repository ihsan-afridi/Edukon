import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App'; // Adjust if your file is located elsewhere

const root = document.getElementById('root');
ReactDOM.createRoot(root).render(<App />);
